# Copy this file to ".env" and adjust as needed.

# Port the API server listens on
PORT=3001

# Path to the JSON data file used for persistence.
DATA_FILE=./tasks.json

# Rate limiting (optional — defaults: 100 requests per 15 minutes per IP)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=100
